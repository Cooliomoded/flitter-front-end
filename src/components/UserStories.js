import React from 'react'

const UserStories = ({ stories }) => {
    console.log(stories)
    return(
        <div>
            Hello from user stories
        </div>
    )
}

export default UserStories